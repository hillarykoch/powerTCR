source("https://bioconductor.org/biocLite.R")
biocLite("BiocStyle")
browseVignettes("BiocStyle")
library(tcR)
?parse.file
?tcR
data(twb)
get.inframes(twb)
get.inframes(twb) -> tt
tt[[1]]
tt[[1]]$Read.count
get.inframes(twb)
tt
tt[[1]]$Read.count
tt <- get.inframes(twb)
tt[[1]]$Read.count
parse.folder
parse.file.list
map(twa, get.inframes())
map(twa, get.inframes
)
library(purrr)
map(twa, get.inframes)
map(twb, get.inframes)
map(twb, get.inframes) %>% map(dim)
map(twb, dim)
length(map(twb, get.inframes))
map(twb, select(Read.count))
?select
map(twb, select("Read.count"))
twb[[1]]$Read.count
select(twb[[1]], "Read.count")
select(twb[[1]], "Read.count") %>% dim
map(twb, "Read.count")
map(twb, "Read.count") -> tt
str(tt)
library(powerTCR)
?parseFile
library(powerTCR)
?fdiscgammagpd
Biocpkg("clonotypeR")
library(powerTCR)
data("repertoires")
fits <- list()
for(i in 1:length(repertoires)){
# Choose a sequence of possible u for your model fit
# Ideally, you want to search a lot of thresholds, but for quick
# computation, we are only going to test 4
thresholds <- unique(quantile(repertoires[[i]], c(.75,.8,.85,.9)))
fits[[i]] <- fdiscgammagpd(repertoires[[i]], useq = thresholds, shift = min(repertoires[[i]]))
}
warnings()
library(magrittr)
for(i in 1:length(repertoires)){
# Choose a sequence of possible u for your model fit
# Ideally, you want to search a lot of thresholds, but for quick
# computation, we are only going to test 4
thresholds <- unique(quantile(repertoires[[i]], c(.75,.8,.85,.9)))
fits[[i]] <- fdiscgammagpd(repertoires[[i]], useq = thresholds, shift = min(repertoires[[i]]))
}
warnings()
fits
names(repertoires)
